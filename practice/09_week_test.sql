-- 1 --
CREATE DATABASE test;
GO
use test;
GO

-- 2 --

CREATE TABLE Product(
	MAKER CHAR(1),
	MODEL CHAR(4),
	TYPE VARCHAR(7)
);
CREATE TABLE Printer(
	CODE INTEGER,
	MODEL CHAR(4),
	COLOR CHAR(1) DEFAULT 'n',
	PRICE DECIMAL(9, 2)
);
CREATE TABLE Classes(
	CLASS VARCHAR(50),
	TYPE CHAR(2)
);

-- 3 --
INSERT INTO PRODUCT
VALUES ('A', '1234', 'Printer'),
	   ('B', '8888', 'Laptop');

INSERT INTO PRINTER
VALUES (55, '1234', 'y', 1244);

INSERT INTO PRINTER(CODE, MODEL)
VALUES(60, '1000');

INSERT INTO CLASSES
VALUES ('Queens', 'bb');

-- 4 --
ALTER TABLE CLASSES
ADD BORE FLOAT;

-- 5 --
ALTER TABLE PRINTER
DROP COLUMN PRICE;

-- 6 --
use movies;
GO
DROP DATABASE test;
GO
